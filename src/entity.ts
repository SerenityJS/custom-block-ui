import { CustomEntityType } from "@serenityjs/core";

// Custom entity type for the container proxy
const ContainerProxyEntityType = new CustomEntityType("serenity:container_proxy");

export { ContainerProxyEntityType };
